body {
    font-family: 'Roboto', sans-serif;
    line-height: 1.5;
    margin: 0;
}
header {
    background: #333;
    color: white;
    padding: 15px;
    text-align: center;
}
.nav-menu {
    display: flex;
    justify-content: center;
    gap: 15px;
}
.hero {
    background: #f5f5f5;
    padding: 50px;
    text-align: center;
}
footer {
    background: #333;
    color: white;
    text-align: center;
    padding: 10px;
}
